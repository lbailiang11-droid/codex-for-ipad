import Foundation

// Compiled directly by both application targets so Xcode supplies the Swift
// runtime and framework link settings used by the shared Swift static library.
@objc(CodexPadLinkAnchor)
final class CodexPadLinkAnchor: NSObject {}
